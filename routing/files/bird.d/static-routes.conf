{% for name, info in salt['pillar.get']('routing:static_routes', {}).items() %}
protocol static static_{{ name | lower }} {
    {% for prefix in info.get('ipv4', {}).get('prefixes', []) %}
    route {{ prefix }} via "{{ info['interface'] }}";
    {% endfor %}
};
{% endfor %}
